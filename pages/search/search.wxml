<van-card wx:for="{{serverList}}" class="padding" bindtap="todetail" data-id="{{item._id}}" price="{{item.price}}" desc="{{item.hospital}}" title="{{item.name}}" thumb="{{ item.image }}">
    <view slot="tags">
        <!-- <view class="cor-red" wx:for="{{List}}">
                {{item.keshiname}}
            </view> -->
        <view class="text1">
            {{item.begood}}
        </view>
    </view>
</van-card>